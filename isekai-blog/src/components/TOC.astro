---
const { headings = [] } = Astro.props;
---

{
  headings?.length > 0 && (
    <details class="toc" open>
      <summary>目次</summary>

      <div class="toc-body">
      <ul>
        {
          headings.map((heading) => (
            <li class={`depth-${heading.depth}`}>
              <a href={`#${heading.slug}`}>
                {heading.text}
              </a>
            </li>
          ))
        }
      </ul>
      </div>
    </details>
  )
}